export type DisplayedImage = {
    image_path: string
    size: string
  }
  
export type DisplayedIndividualImage = {
  id: string
  image_path: string
  name: string
}

export type ImageName = {
  id: string
  name: string
}
